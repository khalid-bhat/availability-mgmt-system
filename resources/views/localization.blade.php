<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    
    <body class="antialiased"> 
    {{__('message.world')}}

   <h1> {{__('message.welcome')}} </h1>

    <p>{{__('message.world')}} </p>
    </body>
</html>
